{
  "name": "angular-cnu",
  "private": true,
  "version": "0.0.1",
  "description": "AngularJS introduction",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    
    "start": "http-server -a localhost -p 5674 -c-1 ./app"
  }
}
